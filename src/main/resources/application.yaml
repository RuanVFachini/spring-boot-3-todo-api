spring:
  application:
    name: api

  security:
    user:
      name: user
      password: user
    auth:
      secret: abc123
      issuer: my-auth

  datasource:
    url: jdbc:h2:file:./data/meubanco
    driverClassName: org.h2.Driver
    username: sa
    password: password

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

#logging:
#  level:
#    root: DEBUG